package FactoryDesignPattern.models;

// for implementing factory design pattern we can create interface or
// abstract class and there will be lots of subclasses which will implements or
// extends Abstract or interface.
public interface Employee {
    int salary();

    String name();
}
